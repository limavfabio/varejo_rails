<%# TODO: Refactor into a responsive sidebar %>
<nav class="col-md-3 bg-white d-md-block sidebar vh-100">
  <div class="sidebar-sticky">
    <h4 class="text-center mt-3">Menu</h4>
    <ul class="nav flex-column">
      <% links = [
        { title: "Home", href: "/", icon: "house" },
        { title: "PDV", href: "/pdv", icon: "cart" },
        { title: "Produtos", href: "/products", icon: "box" },
        { title: "Vendas", href: "/sales", icon: "cash-stack" },
      ] %>

      <% links.each do |link| %>
        <li class="nav-item">
          <a href="<%= link[:href] %>" class="nav-link">
            <%= bi_icon link[:icon] %>
            <%= link[:title] %>
          </a>
        </li>
      <% end %>
    </ul>
  </div>
</nav>
